apiVersion: v1
kind: Service
metadata:
  name: fullstack-service
spec:
  type: NodePort
  selector:
    app: fullstack-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30007